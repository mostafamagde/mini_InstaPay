import 'package:flutter/material.dart';

class SendMoneyView extends StatelessWidget {
  const SendMoneyView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("send money",style: TextStyle(fontSize: 50),),
    );
  }
}
